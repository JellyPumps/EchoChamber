#include "global.h"

int RESOLUTION[2] = {800,600}; // 0=Width, 1=Height
const int FPS = 60;

const float FONT_SIZE = RESOLUTION[0] * 1.0 / 50.0;
const float FONT_SPACING = RESOLUTION[0] * 1.0 / 400.0;

const char *TITLE = "Echo Chamber";